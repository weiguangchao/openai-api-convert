# Domain Docs

Read `CONTEXT.md` and relevant `docs/adr/` entries before exploring the codebase. If absent, proceed silently.

This is a single-context repository: one root `CONTEXT.md` and root `docs/adr/`.

Use terminology from `CONTEXT.md` in issues, proposals and tests. Surface conflicts with existing ADRs instead of silently overriding them.
