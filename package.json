{
  "name": "openai-api-convert",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "start": "node --experimental-strip-types src/index.ts",
    "test": "node --test --experimental-strip-types test/**/*.test.ts",
    "smoke": "node --experimental-strip-types scripts/release-smoke.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
